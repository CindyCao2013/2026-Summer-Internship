# 分钟级因子工程现状快照报告

> 扫描范围：`/home/SiYangCao/factor_dev/factor_research0703/factor_dev`  
> 扫描日期：2026-07-28  
> 方法：全项目文件名/关键词检索 + 核心函数源码阅读 + 本地 Parquet 元数据实测  
> 运行环境参考：`/opt/conda/anaconda3/envs/base_93`

---

## 1. 执行摘要

项目中**已有成熟的「分钟行情 → 日频 brick → 日频 Alpha」管线**，真正意义上的「分钟频因子持续交易/分钟频输出」仍处于 Phase 1 占位阶段。权威分钟源为 DolphinDB `dfs://QV_Trade_to_MinuteBar.Stock_one_minute`（1 分钟 bar，含主动买卖与撤单标签，**无盘口深度**）。本地已落盘约 **25GB** 年切 Parquet（2024–2025 全量）及约 **54GB** 研究缓存。可复用资产集中在：`intraday_lib` 日内回测框架、`l2_data_loaders` / `core/l2_features/*` 的按月 DDB 聚合与缓存、以及 SmartMoney/APM/TGD/IdealAmplitude 等已验证因子。瓶颈主要在**全量分钟 IO、Python 侧 groupby-rolling、缓存重复**；日频数十小时问题与分钟链路部分解耦。**建议优先走「高频低频化」复用路径，而非从零重建分钟因子动物园。**

---

## 2. 数据资产清单

### 2.1 总表

| 数据源名称 | 粒度 | 关键字段 | 时间范围（实测/文档） | 存储位置 | 访问方式 |
|-----------|------|----------|----------------------|----------|----------|
| **QV Stock_one_minute**（权威源） | 1 分钟 bar（tick 聚合 + 主动/撤单标签） | Symbol, Date, Bartime, OHLCV, Amount, Active_buy/sell_{volume,amount,count}, Bid/Ask_cancel_{volume,count}, Adjfactor；（文档另列 Barstart/Barend，本地导出常省略） | 集群历史库；研究常用约 2018/2020–2025 | DolphinDB `dfs://QV_Trade_to_MinuteBar` / `Stock_one_minute` | `dolphindb.session` + `loadTable` / `s.run(sql)` |
| **Fund_one_minute / Cbond_one_minute** | 1 分钟 | 同库基金/转债分钟（本项目因子几乎未用） | 同集群 | 同上库 | 同上 |
| **intraday_2024.parquet** | 1 分钟 | 20 列（见下） | **2024-01-02 → 2024-12-31**；约 **3.09 亿行** / **11.4 GB** | 项目根目录 | `pd.read_parquet` / pyarrow |
| **intraday_2025.parquet** | 1 分钟 | 同上 | **2025-01-02 → 2025-12-31**；约 **3.14 亿行** / **12.2 GB** | 项目根目录 | 同上 |
| **intraday.parquet** | 1 分钟 | 同上 | 约 **2024-05-06 → 2024-06-28** 子集；约 **4980 万行** / **1.7 GB** | 项目根目录 | 同上（不完整，会被按年切分/DDB 补全逻辑覆盖） |
| **月缓存 `.tmp_intraday_YYYY_months/`** | 1 分钟/月 | 同上 | 202401–202412、202501–202512 各 12 文件 | 项目根目录 | 筛选用，避免整年进内存 |
| **smart_money_active_v2/minute_raw** | 1 分钟（连续竞价过滤后） | Symbol→symbol, OHLCV, Adjfactor, Active_* amount/count, cancel vol | **2020-09 → 2024-06**（46 个月文件，**~32 GB**） | `research/cache/smart_money_active_v2/minute_raw/` | builder 缓存 |
| **l2_daily 聚合缓存** | 日频（由分钟 sum） | active_buy/sell vol/amt, cancel vol, volume, amount；另有 imbalance_duration | 如 2021-11-29→2025-12-31（约 515 万行 long） | `research/cache/l2_daily/`（~1.1 GB） | `l2_data_loaders.load_l2_daily_long` |
| **tgd_timing_daily** | 日频（分钟 Close 派生） | Gu, Gd, avg_up/down, R1, R2, n_up/n_down… | 按月缓存约 78 文件（~0.46 GB） | `research/cache/tgd_timing_daily/` | `tgd_panel_builder.load_timing_daily_features` |
| **TGD / APM / Ideal* / bricks 面板** | 日频宽表 | 各因子值 | 依回测窗 | `research/cache/{tgd_panels,apm_*,ideal_*,bricks}/` | 各 `*_builder.py` |
| **WIND EOD / 日历 / 成分股** | 日频 | 开高低收量额、复权、涨跌停、ST、指数权重 | 与因子回测窗对齐 | `dfs://WIND.*` | `factor_data_loaders` / `intraday_lib` DDB |
| **PREHEAT_RET_MATRIX_*** | 分钟点位未来收益 | Ret_15…Ret_180, Ret_EOD, Ret_NDay + Symbol/Date/Bartime | 预热脚本默认约 2020–2026；共享内存表 | DolphinDB SHARED | `data_preheat.preheat_ret_matrix` |
| **ClickHouse `cmds`（HFDATA）** | 配置存在，**本仓库分钟因子链路未接入** | 未在核心路径使用 | — | `DATA_DB_HFDATA`（host 见 COMMON_CONST） | `DB_Demo.py` 示例连接 |
| **真·Level2 / Tick / OrderBook** | — | Bid/Ask 深度、队列等 | **当前集群不可用**（`L2_DATA_LINEAGE.md`） | 空库/无 tablet | 阻塞 L2.3 盘口方向 |

### 2.2 本地分钟 Parquet 字段（20 列，实测）

`Symbol, Date, Bartime, Open, High, Low, Close, Volume, Amount, Active_buy_volume, Active_sell_volume, Active_buy_amount, Active_sell_amount, Active_buy_count, Active_sell_count, Bid_cancel_volume, Bid_cancel_count, Ask_cancel_volume, Ask_cancel_count, Adjfactor`

- **无** Bid1/Ask1、盘口深度、逐笔成交序列。  
- Symbol 样例为 Wind 风格（如 `600000.SH`）；部分 DDB 拉取路径需 `to_wind_code` 规范化。  
- 更新方式：历史静态落盘 + 按需从 DDB **按月增量拉取**（非实时流）。

### 2.3 文档中的 DDB 全量 schema（22 列）

见 `L2_DATA_LINEAGE.md`：在本地 20 列基础上另有 `Barstart` / `Barend`；OHLCV 在 L2 v1/v2 日聚合管线中**未使用**（仅用 Active/Cancel/Volume/Amount）。

---

## 3. 代码文件清单（按功能）

### 3.1 数据加载 / 连接

| 文件 | 作用 |
|------|------|
| `COMMON_CONST.py` | DDB / Oracle / MySQL / ClickHouse 连接常量 |
| `factor_data_loaders.py` | EOD 宽表、`connect_ddb()` |
| `l2_data_loaders.py` | 分钟表 **服务端日聚合** + imbalance_duration + parquet 缓存 |
| `core/l2_features/smart_money_active_v2_builder.py` | `load_minute_active_raw()` 按月拉分钟 Active_* |
| `run_l2_raw_feature_single_factor_screen.py` | `pull_intraday_from_ddb` / `ensure_year_parquet` / `load_intraday` |
| `data_preheat.py` | 预热 `PREHEAT_RET_MATRIX_*`（调用 `get_ret_matrix`） |
| `DB_Demo.py` | 多库连接 demo（含 ClickHouse HF） |

### 3.2 清洗 / Brick / 微观结构

| 文件 | 作用 |
|------|------|
| `factor_cutting/smart_money_active_v2.py` | `apply_minute_qc`（复权、坏 bar）、大单集中度 |
| `intraday_microstructure.py` | EOD proxy bricks + `extract_minute_bricks`（通用分钟砖块，接入浅） |
| `l2_microstructure.py` / `l2_microstructure_v2.py` | VOI/OIR/MPB 日频 brick → 因子 |
| `core/l2_features/bricks/active_pressure/*` | APM 压力 brick（全日/分 session/smart） |
| `core/l2_features/bricks/active_size/*` | 主动规模 brick |
| `core/l2_features/timing_residual.py` / `tgd.py` | TGD 时序中心与残差 |

### 3.3 因子计算（高频 → 日频为主）

| 文件 | 因子/模块 |
|------|-----------|
| `core/l2_features/tgd_panel_builder.py` | **TGD20** |
| `core/l2_features/apm_active_v2_builder.py` | **APM_ActiveV2** 及 Session/Smart/Delta 变体 |
| `core/l2_features/smart_money_*_builder.py` | **SmartMoneyActiveV2** / SmartMoney10d |
| `core/l2_features/ideal_*_active_v2_builder.py` | IdealReversal / IdealAmplitude |
| `core/l2_features/apm_session_panel_builder.py` | Session 切割 APM |
| `factor_formulas_l2.py` / `factor_formulas_l2_v2.py` | L2 注册表 |
| `factor_formulas_*_active_v2.py` | ActiveV2 注册入口 |
| `factor_formulas_l2_flow_p2.py` | 净主动流 × 市值（P2） |
| `intraday_formulas.py` | **真分钟因子清单（Phase2 未实现）** |
| `run_l2_raw_feature_single_factor_screen.py` | 海量分钟特征 → 日 last → IC 初筛 |
| `core/factors/skew/realized_skew.py` | 分钟实现偏度（接口就绪，P0 未接生产） |

### 3.4 存储 / 回测 / 测试入口

| 文件 | 作用 |
|------|------|
| `intraday_lib.py` | DDB 脚本（股票池、复权、**get_ret_matrix**、涨跌停、横截面分组）+ Python 热力图 |
| `intraday_Factortest.py` | 早期日内回测示例（窄表 + 随机时间戳扰动） |
| `Intraday_Factor_Test_Process.py` | 正式 **intraday track** 入口（目前仅 example） |
| `run_p2_intraday_heatmap.py` | 将**日频因子**打到标准 Bartime 上做日内 HML 热力图 |
| `Factor_Test_Process.py` | 日频多 track runner（含 l2 / active_v2） |
| `factor_config.py` | `TRACK`、日期、`INTRADAY_*` 清单、宇宙 |
| `factor_runner.py` / `Factor_Dev_Lib.py` | 日频分组回测、收益矩阵、涨跌停过滤 |

### 3.5 文档与血缘

| 文件 | 作用 |
|------|------|
| `L2_DATA_LINEAGE.md` | 分钟表字段利用与 v2 因子血缘（权威） |
| `research/reports/factors/TGD20/data_source.md` 等 | 单因子数据说明 |
| `docs/milestone_*.md` | APM / ActiveTrade / SmartMoney 里程碑 |

> 说明：按任务要求，单元测试文件（`core/l2_features/test_*.py`）仅作存在性确认，不展开。

---

## 4. 数据处理流程详解

### 4.1 架构全景（两套范式）

```mermaid
flowchart TB
  subgraph SRC["数据源"]
    DDB["DolphinDB\nQV_Trade_to_MinuteBar\nStock_one_minute"]
    WIND["WIND EOD / 日历 / 成分股"]
  end

  subgraph PATH_A["范式 A：真·分钟因子 track（未完成）"]
    IF["intraday_formulas.py\nPhase2 清单占位"]
    IFTP["Intraday_Factor_Test_Process.py"]
    NARROW["窄表 tradetime,symbol,factorname,value"]
  end

  subgraph PATH_B["范式 B：高频低频化（主力生产）"]
    LOAD["按月 DDB 聚合或拉分钟\nl2_data_loaders /\nload_minute_active_raw"]
    BRICK["日频 Brick\nVOI/APM/Smart/TGD GuGd…"]
    PANEL["Date×Symbol 宽表\n+ research/cache"]
    EODBT["Factor_Test_Process\n日频分组回测"]
  end

  subgraph PATH_C["范式 C：日内评估外壳"]
    PRE["data_preheat\nPREHEAT_RET_MATRIX"]
    CS["intraday_lib\nget_cs_group_performance"]
    HM["Bartime × Horizon 热力图"]
  end

  DDB --> LOAD
  DDB --> PRE
  WIND --> BRICK
  WIND --> EODBT
  LOAD --> BRICK --> PANEL --> EODBT
  PANEL -.->|"日频信号 stamp 到 09:59…"|" NARROW
  IF -.->|"待实现"| NARROW
  NARROW --> IFTP --> CS
  PRE --> CS --> HM
  EODBT --> HM
```

### 4.2 路径 B1 — L2 日聚合因子（VOI/MPB/Cancel…）

**输入**：`Stock_one_minute`（服务端 `group by Symbol, Date`）  
**关键函数**：

- `l2_data_loaders.load_l2_daily_long(start, end, …) -> L2DailyLongTable`
- `l2_data_loaders.load_imbalance_duration_daily(...)`（分钟 VOI>0.1 占比）
- `l2_data_loaders.build_l2_daily_cache(...) -> L2DailyWideCache`
- `l2_microstructure_v2`：`cn_voi_shock` 等 = 日 brick + 60d ts_zscore

**清洗**：A 股前缀过滤 `0/3/6`；无逐分钟 QC（拍卖 bar Active 常 NaN，文档已提示）。  
**输出**：`research/cache/l2_daily/*.parquet` → 日频宽表进 `Factor_Test_Process` track `l2_microstructure_v1/v2`。

### 4.3 路径 B2 — SmartMoney / APM / Ideal*（分钟拉数 + Python brick）

**输入**：`load_minute_active_raw` 脚本字段（连续竞价 09:30–11:30 & 13:00–15:00）：

`Open/High/Low/Close/Volume/Amount/Adjfactor + Active_* amount/count + cancel volume`

**清洗**：`apply_minute_qc` — Adjfactor 乘到价格与金额；过滤异常收益/价格；停牌用 volume 掩码；涨跌停在 panel 层 `mask_limit_days`。  
**衍生**：

| 模块 | 日频指标逻辑（摘要） |
|------|----------------------|
| SmartMoneyActiveV2 | 日内 Active_buy/sell_amount 的 top20% 金额占比 → EWM(10) → long−short |
| APM_ActiveV2 | 分钟 `(buy−sell)/(buy+sell)` 按成交额加权 → `apm_raw` → EWM |
| IdealAmplitude | 实现振幅 / 主动净额波动等 → EWM |
| TGD20 | 分钟 Close→Gu/Gd/R1/R2（**DDB 服务端**）+ 隔夜收益残差 → MA20 |

**输出**：`research/cache/{smart_money_active_v2,apm_active_v2,ideal_*,tgd_*}/` 宽/长 parquet。

### 4.4 路径 B3 — 原始分钟特征海选（`run_l2_raw_feature_single_factor_screen.py`）

```
DDB 按月拉取 → year/month parquet
  → normalize_minute_df（Wind 码、0/3/6、连续竞价 9:31–11:30 & 13:01–15:00）
  → build_base_features（15 个比率/失衡/LogReturn/HighLowSpread…）
  → build_rolling_features（对象×窗{5,10,30}×op{mean,std,max,min,skew,last_diff}）
  → build_big_order_features（相对 240 分钟均量 2× 大单 mask）
  → minute_to_daily_last（每日最后一个 Active 有效 bar）
  → Factor_Dev_Lib IC / 十分组
  → research/results/l2_raw_feature_screen/
```

已产出约 50 个特征名、`selected_factors.csv` / `final_good_factors.csv`（以 Active* 失衡与量额 rolling 为主）。

### 4.5 路径 A/C — 真分钟回测与热力图

**信号窄表约定**（`intraday_Factortest.py` docstring）：

`tradetime | symbol | factorname | value`  
推荐计算点位：`09:59, 10:29, 10:59, 11:29, 13:29, 13:59, 14:29`

**收益定义**（`intraday_lib.get_ret_matrix`）：

- 价格：后续 `vwap_min_window`（默认 5）分钟 VWAP × 复权因子，再 shift  
- 标签：`Ret_15/30/60/90/120/150/180`、`Ret_EOD`（相对 14:50 VWAP）、`Ret_NDay`（约 +239 bar）  
- 文档性能注记：中证 1000、3 年约 **2 分 15 秒**（DDB）

**评估**：`get_cs_group_performance(signal, ret_matrix, group_num=5)` → HML / Rank IC / 热力图。

**现状**：`intraday_formulas.INTRADAY_FACTOR_LIST` 中 Phase2 名称均 **SKIP / 未注册**；`Intraday_Factor_Test_Process` 仅用 EOD rolling 映射到 09:59 的 `intraday_example`。`run_p2_intraday_heatmap` 则是把**已有日频 L2 流因子**复制到多个 Bartime，验证「何时进场」而非分钟因子本身。

### 4.6 配置项与默认值

| 配置 | 位置 | 默认（当前文件） |
|------|------|------------------|
| `TRACK` | `factor_config.py` | `"ideal_amplitude_active_v2"` |
| `START_DAY` / `END_DAY` | 同上 | 2021-01-01 → 2024-06-30 |
| `PREHEAT_CALENDAR_DAYS` | 同上 | 400 |
| `UNIVERSE_LIST` | 同上 | ALL / CSI1000 / CSI500 / CSI300 |
| `INTRADAY_PHASE1_LIST` | `intraday_formulas.py` | `["intraday_example"]` |
| 筛选用起止 | `run_l2_raw_feature_*.py` | 2024-01-01 → 2025-12-31 |
| DDB 数据连接 | `COMMON_CONST.DATA_DB_CONN` | host `10.12.180.9:8902`（只读账号） |
| 涨跌停过滤（日频） | `Factor_Dev_Lib.get_EOD_Not_Limit` | ActiveV2 回测必用 |

---

## 5. 性能热点分析

> 以下为**静态代码评估 + 数据规模推断**，非本次实测 profiler。

| 环节 | 原因 | 估算占比（量级） |
|------|------|------------------|
| **整年分钟 Parquet 读入** | 单年 ~3e8 行 / 11–12GB；`load_intraday` 全表 `read_parquet` 易 OOM | IO **25–40%**（若走年文件路径） |
| **Python groupby-rolling 特征** | `_roll_feature` / 大单循环按 Symbol 切分；特征数可达百级 | CPU **30–45%**（海选脚本） |
| **DDB 全量分钟拉取** | 按月仍大；网络 + 反序列化到 pandas | 冷启动 **20–35%** |
| **重复分钟缓存** | `intraday_*.parquet`（~25GB）与 `smart_money…/minute_raw`（~32GB）字段高度重叠 | 磁盘/冷启冗余，可省 **数小时** 重复拉取 |
| **日频全量因子动物园** | EOD track 数十小时：多因子 × 多宇宙 × 绘图/IC；与分钟 IO **部分解耦** | 属另一瓶颈面 |
| **相对较快** | DDB 侧 `sum/group by`、`get_ret_matrix` 分 30 天桶、TGD timing 服务端聚合、月 parquet 缓存命中 | 热路径缓存命中后 brick 构建可压到分钟级～十分钟级 |

**并行化潜力（高）**：

- 按 **自然月 / 股票分区** 已存在切分点（`_month_starts`、`.tmp_*_months`）。  
- 可按月多进程拉数或特征（需注意 DDB 连接数与写缓存冲突）。  
- 日频 IC/分组可对因子维并行（现有 `factor_runner` 偏串行 batch）。

**并行化风险**：多进程同时写同一 `research/cache`；DDB `pyread` 并发限流未在代码中显式管理。

---

## 6. 可复用模块与待开发模块

### 6.1 已有（建议直接复用）

| 类别 | 资产 |
|------|------|
| 连接与 EOD | `connect_ddb`、`load_eod_wide_tables` / `load_eod_enriched_tables` |
| 分钟权威读 | `l2_data_loaders` 日聚合；`load_minute_active_raw`；`pull_intraday_from_ddb` |
| QC | `apply_minute_qc`、连续竞价过滤、A 股 `0/3/6`、涨跌停 `mask_limit_days` / `get_EOD_Not_Limit` |
| Brick | VOI/OIR/MPB、active_pressure、active_size、TGD Gu/Gd |
| 成熟因子 | TGD20、SmartMoneyActiveV2、APM_ActiveV2、IdealAmplitude/Reversal、L2 v2 shocks |
| 日内评估 | `intraday_lib.ddb_functions`、`get_cs_group_performance`、热力图工具、`preheat_ret_matrix` |
| 日频回测外壳 | `Factor_Test_Process` + `factor_config.TRACK` + `Factor_Dev_Lib` |
| 缓存约定 | `research/cache/**` 按月 parquet + factor_panel |

### 6.2 缺失 / 薄弱（分钟因子构建必需）

| 缺口 | 说明 |
|------|------|
| **真分钟 Alpha 公式实现** | `intraday_formulas` Phase2（尾盘强度、VWAP 偏离、成交前后置等）未实现；`INTRADAY_REGISTRY` 为空 |
| **分钟级 ST/涨跌停交易约束** | `intraday_Factortest` TODO 明确提出；仅有 `get_limit_status` DDB 函数，未接入回测主路径 |
| **统一分钟数据层** | 多套拉数脚本列名不一致（`Active_buy_amount` vs `active_buy_amt`）；缓存重复 |
| **隔夜跳空标准化模块** | TGD 有 overnight residual；通用分钟因子层缺失 |
| **盘中重采样框架** | 无统一 5m/15m resample API；仅有固定 Bartime 抽样约定 |
| **分钟频标准化/行业中性** | 日频有 `industry_neutral` / cs_zscore；分钟横截面工具未产品化 |
| **增量日更流水线** | 多为研究窗口全量/按月；无生产级「昨日分钟 → 更新 brick」调度 |
| **盘口/真 Tick** | 集群无 SSL2/OrderBook；不可做真 OFI/队列 alpha |
| **ClickHouse HF** | 仅有连接常量，未纳入因子图 |
| **实现偏度 RSKEW** | 函数在 `realized_skew.py`，未接入主 runner |
| **复权一致性** | 筛选用原始 Close 比 Open；ActiveV2 用 Adjfactor；`get_ret_matrix` 用日频 Adj_Factor — 需统一口径 |

---

## 7. 风险提示与注意事项

1. **数据权限与版权**：分钟与 WIND 数据来自内部 DolphinDB/中信源库；报告与交付勿外泄连接凭证（`COMMON_CONST` 含明文口令，仓库治理需注意）。  
2. **时区/时间戳**：交易日为 A 股日历；`Bartime` 部分 parquet 可能落在 epoch 日，需与 `Date` 拼接（筛选用脚本已处理）。  
3. **复权不一致**：见上；跨模块拼接因子时务必核对是否已乘 `Adjfactor`。  
4. **集合竞价 / 15:00 bar**：Active_* 常 NaN；日 last 聚合需过滤，否则特征塌陷。  
5. **北交所代码变更**：`map_bj_code` 仅在 `get_ret_matrix_BJ` 等路径；主流 A 股 ActiveV2 过滤不含 BJ。  
6. **「L2」命名易误导**：当前为 **trade-flow 标签分钟 bar**，非订单簿 Level-2。  
7. **预热共享表生命周期**：`PREHEAT_RET_MATRIX_*` 依赖 DDB 会话/共享内存；进程重启需重跑 `data_preheat.py`。  
8. **pandas 版本偏旧**：`base_93` 为 **pandas 1.2.4 / numpy 1.20.1**，与新 API（`dropna`/`groupby` 行为）及内存效率有差距；大规模分钟处理建议评估升级或 Polars（环境已有 polars 0.20.1 但核心路径未用）。  
9. **磁盘**：仅分钟相关缓存+落盘已超 **50GB+**；继续复制年文件前应先统一缓存策略。

---

## 8. 下一步工作建议（已确认执行路线）

> **战略选择**：优先聚焦「高频低频化」的深化与工程化，**而非**从零搭建分钟级因子动物园。  
> 目标：在不显著增加公式量的前提下，快速获得分钟维度对现有策略的增强价值，并消除分钟部分的 IO/可扩展性瓶颈。

### 8.1 P0 — 统一分钟数据层（消除最大瓶颈）

**问题**：三套字段高度重叠的分钟缓存（`intraday_2024/2025.parquet`、`smart_money_active_v2/minute_raw`、`.tmp_*_months`），列名不一致，冷启动 IO 冗余严重（~25GB + ~32GB）。

**动作**：

1. 封装 `MinuteBarStore`：统一从 DolphinDB / 月 Parquet 读取，输出规范列名（如 `active_buy_amt`）。
2. 废弃或软链重复文件，合并为**单一真相源**，磁盘占用目标减半。
3. 加入**交易日级增量追加**：每次仅拉取新交易日分钟数据并更新月缓存，改变「每次全量拉数年」模式。

**工期目标**：短期 1–2 周。

### 8.2 P1 — 复用成熟日频 Brick 做「分钟择时」诊断

**逻辑**：TGD20、SmartMoneyActiveV2、APM_ActiveV2 等已稳定；缺的是**哪个日内时点信号最有效**的量化证据。

**动作**：

1. 扩展 `run_p2_intraday_heatmap.py` 模式，将上述因子 stamp 到标准 Bartime（`09:59, 10:29, 11:29, 13:29, 14:29` 等）。
2. 计算各时点对未来多窗口收益的 IC / HML，输出热力图矩阵。
3. 直接指导实盘时点（例如「APM 在 10:29 最强」）——**无需开发新因子**。

### 8.3 P1 — 补齐 2～3 个关键真分钟 Alpha

**现状**：`intraday_formulas.py` Phase2 全为占位；`Intraday_Factor_Test_Process` 回测外壳已就绪。

**优先实现**：

| 因子名 | 语义 |
|--------|------|
| `close_vwap_deviation` | 收盘价相对日内 VWAP 偏离 |
| `late_session_strength` | 尾盘 30 分钟主动买强度 |
| `volume_front_loading` | 早盘成交量占全天比例 |

**接入**：窄表 `tradetime|symbol|factorname|value` → 现有日内回测 → Rank IC + 分组收益。

### 8.4 P1 — 补充风控与交易约束

- 接入已有 `get_limit_status`，实现分钟级涨跌停 / ST 过滤（关闭 `Intraday_Factor_Test_Process` TODO）。
- **统一复权口径**：分钟因子一律 `Adjfactor` 乘到价格/金额，并与 `get_ret_matrix` 日频复权对齐，避免收益虚高。

### 8.5 P2 — 性能攻坚（计算下沉与并行化）

针对 `run_l2_raw_feature_single_factor_screen` 的 Python groupby-rolling 热点：

- 滚动特征（mean/std/skew）下沉 DolphinDB，或迁 Polars（环境已有 0.20.1）。
- **按月分区**多进程拉取与特征计算，最后 concatenate。
- 保持 `PREHEAT_RET_MATRIX_*` 共享（约 2 分 15 秒 / 3 年），避免重复预热。

**工期目标**：长期 1 个月+（与生产级分钟流水线探索一并推进）。

### 8.6 暂缓方向（明确不做）

| 暂缓项 | 原因 |
|--------|------|
| 等待真实 OrderBook / 盘口 | 当前集群不可用，会阻塞全部 OFI/队列类因子 |
| 从零构建分钟因子动物园 | 已有 50+ 分钟特征初筛，应优先消化、择时化、工程化 |
| 引入第三套分钟缓存格式 | 统一存储是前提；再多一套只会恶化维护 |

### 8.7 执行时间线

| 阶段 | 窗口 | 交付 |
|------|------|------|
| 短期 | 1–2 周 | `MinuteBarStore` + 增量更新 → 消除最大 IO 瓶颈 |
| 中期 | 2–4 周 | 分钟择时热力图（TGD/APM/SmartMoney）+ 涨跌停过滤 + 2～3 个真分钟 Alpha |
| 长期 | 1 个月+ | DDB/Polars 下沉、剩余 Phase2 按需补全、生产级分钟流水线 |

### 与日频流程的异同（决策相关）

| 维度 | 日频 EOD | 分钟相关现状 |
|------|----------|--------------|
| 主入口 | `Factor_Test_Process` | 生产 alpha 仍进该入口；真分钟用 `Intraday_*` |
| 输出形态 | Date×Symbol 宽表 | 主流已压缩为日频；窄表仅评估 |
| 收益标签 | c2c/o2o/v2v | VWAP 前瞻多 horizon |
| 可复用 | 分组、IC、行业中性、涨跌停 | 共用 `Factor_Dev_Lib` / `intraday_lib` 股票池 |

**结论**：按上述路径，可在不显著增加公式量的前提下，快速获得分钟维度对现有策略的增强价值，并从根本上解决「数十小时」流程中**分钟部分**的可扩展性问题。

---

## 附录 A. 关键代码调用关系图

```text
COMMON_CONST.DATA_DB_CONN
        │
        ├─ factor_data_loaders.connect_ddb
        │         │
        │         ├─ l2_data_loaders.build_l2_daily_cache
        │         │         └─ l2_microstructure(_v2) → factor_formulas_l2(_v2)
        │         │                   └─ Factor_Test_Process (track l2_*)
        │         │
        │         ├─ smart_money_active_v2_builder.load_minute_active_raw
        │         │         ├─ smart_money_active_v2_builder → SmartMoneyActiveV2
        │         │         ├─ bricks.active_pressure.daily_builder → apm_active_v2_builder
        │         │         ├─ ideal_amplitude_active_v2_builder
        │         │         └─ ideal_reversal_active_v2_builder
        │         │                   └─ Factor_Test_Process (track *_active_v2)
        │         │
        │         └─ tgd_panel_builder.load_timing_daily_features
        │                   └─ build_tgd20_wide_from_eod_l2 → 研究报告 / delivery
        │
        ├─ run_l2_raw_feature_single_factor_screen.pull_intraday_from_ddb
        │         └─ 特征海选 → research/results/l2_raw_feature_screen
        │
        ├─ data_preheat.preheat_ret_matrix
        │         └─ intraday_lib.get_ret_matrix → SHARED PREHEAT_RET_MATRIX_*
        │
        ├─ Intraday_Factor_Test_Process / intraday_Factortest
        │         └─ filter_in_index + get_cs_group_performance + 热力图
        │
        └─ run_p2_intraday_heatmap
                  └─ 日频因子 stamp Bartime → 同上热力图路径
```

## 附录 B. 关键库版本（`base_93` 环境实测）

| 库 | 版本 |
|----|------|
| Python（环境） | 以 conda `base_93` 为准 |
| pandas | 1.2.4 |
| numpy | 1.20.1 |
| dolphindb | 3.0.3.0 |
| pyarrow | 14.0.2 |
| dask | 2021.4.0（核心分钟路径未深度使用） |
| sqlalchemy | 1.4.7 |
| polars | 0.20.1（已装未进主路径） |
| scipy | 1.10.0 |
| matplotlib | 3.3.4 |
| oracledb | 3.0.0 |

## 附录 C. 扫描假设（若与现场不符请确认）

1. 权威分钟源仅为当前 DDB 集群中的 `QV_Trade_to_MinuteBar`；ClickHouse `cmds` 未参与因子生产。  
2. `intraday.parquet` 为过渡子集，**不以它作为 2024 全年覆盖依据**（以 `intraday_2024.parquet` 为准）。  
3. 真 OrderBook/Tick 库在本集群仍不可用（依据 `L2_DATA_LINEAGE.md` 2026-07-08 审计，未在本次重连校验）。  
4. 「日频数十小时」瓶颈主要来自 EOD 多因子全量回测，分钟链路优化能显著缩短 **brick 构建与特征海选**，但不自动消除全部 EOD 回测耗时。

---

*报告结束。*
